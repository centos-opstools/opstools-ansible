---

- name: add port {{ rabbitmq_port }}
  set_fact:
      firewall_ports: "{{ firewall_ports }} + ['{{ rabbitmq_port }}']"

- name: add port {{ uchiwa_port }}
  set_fact:
      firewall_ports: "{{ firewall_ports }} + ['{{ uchiwa_port }}']"
